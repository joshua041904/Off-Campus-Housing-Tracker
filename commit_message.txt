chore(gitignore): ignore PR paste local copy + preflight telemetry artifacts

Why
---
- PR review snippets are dev convenience; keep a tracked **template** in-repo but do not
  force every clone to commit local wordings.
- Preflight writes telemetry/metrics snapshots to the repo root by default; those files are
  local noise and should not appear in `git status`.

Changes
-------
- .gitignore:
  - docs/PR_REVIEW_GRPC_HANDLER_PASTE.txt (optional local copy; edit without tracking)
  - telemetry-during-*.log, telemetry-after-*.txt, raw-metrics-*.txt, live-telemetry.csv,
    perf-*.data, htop-after-*.txt (run-preflight-scale-and-all-suites.sh outputs)
- docs/PR_REVIEW_GRPC_HANDLER_PASTE.example.txt: tracked template (same content as before)
- Removed docs/PR_REVIEW_GRPC_HANDLER_PASTE.txt from the tree (use example + local copy)
- README.md, docs/GRPC_ONBOARDING.md, services/listings-service/README.md: link to
  **.example.txt**; document gitignored PASTE.txt

Follow-up (if these were already committed)
-------------------------------------------
  git rm --cached telemetry-after-*.txt telemetry-during-*.log raw-metrics-*.txt 2>/dev/null || true
  git rm --cached docs/PR_REVIEW_GRPC_HANDLER_PASTE.txt 2>/dev/null || true

Local workflow
--------------
  cp docs/PR_REVIEW_GRPC_HANDLER_PASTE.example.txt docs/PR_REVIEW_GRPC_HANDLER_PASTE.txt
  # edit PASTE.txt — stays untracked
