perf(perf-lab): cluster contention story, k6 pacing hooks, listings diagnostics, green-team docs, preflight hardening

WHY (cluster-level resource contention — not “fix listings replicas”)
----------------------------------------------------------------------
Messaging / analytics / listings can look “sick” in a **long suite** while the **same paths are fine in isolation**:
  • Back-to-back k6 (especially **constant-arrival-rate**), high VUs, shared Colima/k3s CPU, virtiofs + hypervisor scheduling.
  • **Envoy** retains connection state; **Postgres** spikes when many services load together.
  • **Listings “got better” after enabling HTTP timing** partly because **rollout restart** reset pools / memory / event-loop state — stability signal, not proof the code was wrong.

**Prove it (second terminal while preflight or edge smoke runs):**
  kubectl top pods -n off-campus-housing-tracker
  kubectl top nodes
Watch: CPU or memory **> ~80%**, any pod **≥ ~1 core** sustained, Postgres- or Envoy-shaped spikes. Hooks also **warn** at 80% (K6_SUITE_WARN_*) and can **fail** at **85%** node CPU (K6_SUITE_NODE_CPU_MAX).

WHAT WE DO (orchestration + visibility — maps to “do this properly”)
--------------------------------------------------------------------
Implemented in **scripts/lib/k6-suite-resource-hooks.sh** and wired into **run-housing-k6-edge-smoke.sh**, **load/run-k6-phases.sh**, **run-all-test-suites.sh**, **run-preflight-scale-and-all-suites.sh**:

  1) **Cooldown after every k6 block** — default **K6_SUITE_COOLDOWN_SEC=15** (GC, pools, sockets).
  2) **Extra settle after CAR-style tests** — **K6_SUITE_CAR_EXTRA_SEC=20**; optional **K6_SUITE_RESTART_ENVOY_AFTER_CAR=1** (+ sleep, disruptive) or **K6_SUITE_STABILITY_AGGRESSIVE=1**.
  3) **Log kubectl top nodes + pods** after each block — **K6_SUITE_LOG_TOP=1**; optional **K6_SUITE_LOG_TOP_BEFORE=1**; append to **bench_logs/k6-suite-resources-*.log** via **K6_SUITE_RESOURCE_LOG** / **K6_SUITE_RESOURCE_LOG_AUTO** (preflight 7a enables AUTO).
  4) **Fail when node CPU too high** — **K6_SUITE_FAIL_ON_NODE_CPU=1**, **K6_SUITE_NODE_CPU_MAX=85**; hook **exit code 3**. Preflight **fails the run** on rc=3 after service grid / after full grid hook / after 7c in-cluster k6 (not only warn). Set **K6_SUITE_FAIL_ON_NODE_CPU=0** for noisy labs or missing metrics-server.

**Optional harsh lab (Colima VM):** **K6_SUITE_COLIMA_DROP_CACHES=1** — before each k6 block, **colima ssh** runs **sync** + **drop_caches** (same idea as manual sync; sudo inside VM). Not for production.

**Preflight-specific:** Sources hooks at start of **7a**; after **run-housing-k6-edge-smoke.sh** runs **k6_suite_after_k6_block preflight-after-k6-service-grid** (extra top + cooldown before Playwright).

OTHER CHANGES IN THIS INTEGRATED PR
-----------------------------------
  • **Listings HTTP diagnostics** — **services/listings-service/src/http-server.ts**, **infra/k8s/base/listings-service/deploy.yaml**, **docs/perf/LISTINGS_*.md**, **TAIL_LATENCY_AND_CROSS_SERVICE_ANALYSIS.md**.
  • **k6** — **k6-strict-edge-tls.js**, **k6-listings-health.js**, thresholds / **x-loadtest** on touched scripts.
  • **Gateway / transport / housing** — **api-gateway/server.ts** (JSON **4xx** vs 500), **packet-capture-v2.sh**, **test-microservices-http2-http3-housing.sh**.
  • **Perf** — **scripts/perf/** EXPLAIN + k6 report scripts, **run-k6-cross-service-isolation.sh**, **watch-cluster-contention.sh**, **CLUSTER_CONTENTION_WATCH.md**.
  • **Docs / PR body** — **GITHUB_PR_DESCRIPTION.txt** single canonical paste (review split; **§3** prerequisites; **§4** first-time / green team + **§4.7** Ollama + **§4.11** one-paste; **§6** testing map). **docs/PR_SECOND_ONBOARDING.md** aligned. **GITHUB_PR_DESCRIPTION_SECOND.txt** stub → use main PR file only.
  • **Docs rename** — **`docs/CERTS_AND_TESTING_FOR_MORTALS.md`** → **`docs/LOCAL_TLS_AND_TESTING_GUIDE.md`** (neutral title); README, Runbook, **FIRST_TIME_TEAM_SETUP**, **RUN_PIPELINE_ORDER**, **PR_SECOND_ONBOARDING**, PR body links updated.
  • **Shell** — **run-preflight-scale-and-all-suites.sh** Phase **A/C** closing fix (no stacked **fi** parse issue); **_phase_a_only** exit uses **&& { … }** group; **say** lines with parentheses use **single-quoted** messages + ASCII **`...`** (avoids subshell/parse edge cases around **`(`** / Unicode ellipsis on some setups).

WHERE TO READ / PASTE
---------------------
  • Narrative: **docs/perf/TAIL_LATENCY_AND_CROSS_SERVICE_ANALYSIS.md**, **docs/perf/CLUSTER_CONTENTION_WATCH.md**
  • GitHub PR: **GITHUB_PR_DESCRIPTION.txt**
  • Listings operator: **docs/perf/LISTINGS_HTTP_TIMING.md**

HOW TO RUN (cheat sheet)
------------------------
  # Second terminal — prove contention
  kubectl top nodes
  kubectl top pods -n off-campus-housing-tracker

  # Continuous top → file
  ./scripts/perf/watch-cluster-contention.sh

  # Edge k6 grid (hooks between scripts)
  SSL_CERT_FILE="$PWD/certs/dev-root.pem" ./scripts/run-housing-k6-edge-smoke.sh

  # Preflight (relax CPU gate if Colima too noisy)
  K6_SUITE_FAIL_ON_NODE_CPU=0 RUN_PGBENCH=0 ./scripts/run-preflight-scale-and-all-suites.sh

  # Green team first machine — see GITHUB_PR_DESCRIPTION §4.1–4.8
  ./scripts/setup-new-colima-cluster.sh
  RESTORE_BACKUP_DIR=latest ./scripts/bring-up-external-infra.sh
  # … TLS §4.6, then bring-up-cluster-and-infra / preflight

HOOK ENV (reference)
--------------------
  K6_SUITE_COOLDOWN_SEC=15 K6_SUITE_CAR_EXTRA_SEC=20
  K6_SUITE_LOG_TOP=1 K6_SUITE_FAIL_ON_NODE_CPU=1 K6_SUITE_NODE_CPU_MAX=85
  K6_SUITE_WARN_HOT_RESOURCES=1 K6_SUITE_WARN_NODE_CPU=80 K6_SUITE_WARN_NODE_MEM=80
  K6_SUITE_RESTART_ENVOY_AFTER_CAR=0 K6_SUITE_ENVOY_RESTART_SLEEP_SEC=10
  K6_SUITE_STABILITY_AGGRESSIVE=0 K6_SUITE_COLIMA_DROP_CACHES=0
  K6_SUITE_LOG_TOP_BEFORE=0 K6_SUITE_RESOURCE_LOG_AUTO=1

NEXT (backlog)
--------------
  • Isolation matrix: which prior k6 script poisons the next (cross-service order).
  • Tune CAR / VUs to sustainable RPS on Colima 12c/16Gi.
  • Correlate **K6_SUITE_RESTART_ENVOY_AFTER_CAR** with improved tails when enabled.

VERIFY BEFORE PUSH
------------------
  bash -n scripts/lib/k6-suite-resource-hooks.sh
  bash -n scripts/load/run-k6-phases.sh
  bash -n scripts/run-housing-k6-edge-smoke.sh
  bash -n scripts/run-preflight-scale-and-all-suites.sh
  SSL_CERT_FILE="$PWD/certs/dev-root.pem" ./scripts/run-housing-k6-edge-smoke.sh

DEPLOY NOTES
------------
  • Rebuild + rollout **api-gateway** if JSON 4xx behavior was validated in-cluster.
  • Rebuild + load + rollout **listings-service** for HTTP timing logs (env alone insufficient).

Suggested subject
-----------------
perf(perf-lab): k6 suite contention hooks, preflight CPU fail + post-grid settle, listings diagnostics, green-team PR doc
