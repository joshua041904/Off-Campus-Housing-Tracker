ci(och): och-ci contract, certification script, H2 anomaly gate, port + QUIC invariants

WHY
---
  • Lock separation: docker-compose (infra), k3s (runtime), GitHub CI (no private cluster),
    local make certify (full edge + transport-lab + declare-readiness).
  • Remove phantom failures: pnpm without action-setup, strict Playwright on unreachable edge,
    local-stack workflow implying wrong architecture.
  • Enforce HTTP/2 matrix collapse in CI before spending matrix minutes; align declare-readiness
    with extract-protocol-matrix output when transport artifact omits protocol_integrity.

WHAT CHANGED
------------
  • .github/workflows/ci.yml — och-ci: protocol-anomaly → build → test → transport-validation →
    quic-hostname-invariant; playwright-strict if vars.RUN_STRICT_PLAYWRIGHT == true; OCH matrices.
  • .github/workflows/docker-build.yml — och-docker-build matrix (services + webapp).
  • Remove .github/workflows/local-integration.yml.
  • .github/workflows/protocol-validation.yml — shellcheck CI scripts; pnpm 9.11.0; path filters.
  • scripts/ci/run-full-certification.sh — certify order; CI=true exits after pnpm test; docker
    required when not CI.
  • scripts/ci/verify-docker-ports.sh — host ports 5441–5448, 6380 (docker ps).
  • scripts/ci/verify-quic-hostname-invariant.sh — no raw-IP https BASE_URL defaults in scripts/load.
  • scripts/perf/extract-protocol-matrix.js — EXTRACT_PROTOCOL_MATRIX_FROM_CSV CSV fallback; comment fix.
  • scripts/protocol/declare-readiness.js — fold in protocol-matrix-anomalies.json when needed.
  • Makefile — certify, verify-docker-ports.
  • webapp — PLAYWRIGHT_STRICT_HTTP3 on strict e2e scripts; system-integrity state assertions.
  • docs/LOCAL_DEV.md — CI section matches och-ci / removed local workflow.

NOT IN THIS COMMIT
------------------
  • Fixing any_http2_collapse_anomaly (pool / H2 tuning — separate performance work).
  • python-ai-service Docker matrix row (no Dockerfile in tree yet).

RUN
---
  • make verify-docker-ports
  • make certify
  • EXTRACT_PROTOCOL_MATRIX_FROM_CSV=1 node scripts/perf/extract-protocol-matrix.js

Suggested subject
-----------------
ci(och): deterministic och-ci, certify script, collapse + port + QUIC invariants

Made with Tom.
