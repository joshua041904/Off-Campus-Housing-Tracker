feat(preflight): run media-service Vitest alongside messaging in step 7a

Why
---
- Preflight must exercise both **services/messaging-service/tests/** and
  **services/media-service/tests/** so DB + handler integration runs in CI/local
  the same way as messaging (Vitest before shell HTTP/2|3 suites).

Changes
-------
- scripts/run-preflight-scale-and-all-suites.sh: after messaging `pnpm test`,
  run **pnpm -C services/media-service test** (fails preflight on non-zero exit).
- services/media-service/tests/setup/env.ts: set **PG_HOST**, **PG_PORT** (5448),
  **PG_DATABASE** (media), credentials for integration tests on host Postgres.
- services/media-service/vitest.config.ts: **setupFiles** → `./tests/setup/env.ts`
  (removes ambiguous PG_HOST warning; matches messaging-service pattern).
- services/media-service/tests/integration/media-upload.integration.test.ts: fix
  comment (media DB is 5448, not 5441).
- README.md + docs/CERTS_AND_TESTING_FOR_MORTALS.md: document dual Vitest in 7a.

Verified (local, Colima)
------------------------
- pnpm -C services/messaging-service test — pass (3 tests)
- pnpm -C services/media-service test — pass (4 tests, setup ~97ms)
- PREFLIGHT_APP_SCOPE=core RUN_MESSAGING_LOAD=0 test-microservices-http2-http3-housing.sh — exit 0
  (expected 504 on non-deployed listings/booking/trust/analytics/media edge health when scope is core;
  messaging/auth/gateway paths green)
- test-social-service-comprehensive.sh — exit 0, all messaging/forum checks passed

How to run
----------
- Full Vitest gate only:
    pnpm -C services/messaging-service test
    pnpm -C services/media-service test
- With preflight (core app deploys):
    PREFLIGHT_APP_SCOPE=core ./scripts/run-preflight-scale-and-all-suites.sh

Prereqs for media Vitest
------------------------
- Postgres **media** listening on **127.0.0.1:5448** (e.g. docker compose postgres-media).
