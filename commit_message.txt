perf(lab): canonical 10-file perf bundle, k6 protocol matrix, and edge TLS protocol env

WHAT
----
  • scripts/perf/build-canonical-bundle.sh — from bench_logs/run-<STAMP>/ copies 10 fixed names into
    PERF_CANONICAL_10/, writes summary.json (sha256 per file), zips perf-bundle-<STAMP>.zip.
  • scripts/load/run-k6-protocol-matrix.sh — sequential gateway-health k6 runs: TLS ALPN (default),
    HTTP/1.1 hint (GODEBUG=http2client=0 + K6_PROTOCOL=http1), HTTP/3 via .k6-build/bin/k6-http3 when present;
    outputs protocol-matrix/*/summary.json and protocol-comparison.md (via summarize-protocol-matrix.sh).
  • scripts/load/k6-gateway-health-http3.js — /api/healthz using k6/x/http3 when the custom binary is used.
  • scripts/load/k6-strict-edge-tls.js — K6_PROTOCOL / K6_HTTP_PROTOCOL (auto|http1|http2|http3), keep-alive,
    k6_protocol tag, httpVersion for h1/h2 where k6 supports it.

DOCS / TEAM
-----------
  • TEAM_COMMANDS_IN_ORDER.txt — copy/paste for build-canonical-bundle and run-k6-protocol-matrix.
  • GITHUB_PR_DESCRIPTION.txt — §4.10, §6.3/6.4/6.8, §7.5 recipes + exec summary row.

Suggested subject
-----------------
perf(lab): canonical perf zip + k6 h1/h2/h3 matrix and protocol env on edge TLS helper
