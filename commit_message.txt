feat(och): edge transport observability, booking/notifications UX, CI bootstrap fix

Edge protocol (Caddy → api-gateway)
- Terminate h1/h2/h3; stamp X-OCH-Edge-Proto; gateway metrics and OTEL spans label
  client→edge proto (h1|h2|h3), not the internal Caddy→gateway hop.
- Grafana transport dashboard, Prometheus unknown-proto alert, obs smokes and proof artifacts.

Booking and renter dashboard
- GET /bookings/mine views (active, past, all, dashboard) with OR-branch view filters.
- Recent bookings on search dashboard: load view=all, show all confirmed/pending_confirmation
  upcoming rows (matches My bookings); exclude draft created integration noise.
- My bookings page: upcoming vs past partition; listing images and host/renter labels.

Landlord notifications
- Booking-context mark-read syncs lifecycle siblings (created/confirmed/cancelled) in API + UI.
- Human-readable event labels (“Booking confirmed”, not booking.confirmed); drop session read
  overlay that fought API state after refresh.
- Notification list cache invalidation; collapsed unread counts; landlord dashboard load helpers.

Auth and platform
- Login resolves merged/deleted alias emails to canonical user id.
- Colima rebuild helpers, Docker probe installs, infra DB migration scripts and notification
  read-state SQL (27–31).

CI / integration tests
- Add infra/db/04-booking-search-history.sql (search_history + watchlist) and apply 04/05/06
  before 19-booking-search-history-alerts.sql in bootstrap-all-dbs.sh and ensure-booking-schema.sh
  so SQL-only CI bootstrap no longer fails with “relation booking.search_history does not exist”.
- auth-service: partial @common/utils mock (importOriginal) for och-outbox metrics exports.
- api-gateway: drop bare /api/listings from 502 open-route matrix; longer Vitest timeouts.
- ci.yml: OCH_DISABLE_EXTERNALS on integration batch; SLO/Playwright need build only; gate
  always requires playwright-strict success.

Verified: webapp unit tests (booking partition, notification projection/read persistence);
booking-service mine-view tests; notification mark-context-read tests.
