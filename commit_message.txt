fix(infra): auth-service K8s ports/namespace, Colima namespaces-only, 7-DB backup rename, build-and-load auth

Auth-service (infra/k8s/base/auth-service)
-------------------------------------------
- deploy.yaml, service.yaml, grpc-health-probe.yaml: namespace record-platform → off-campus-housing.
- service.yaml: align with README exposed ports — http 4001→4011, grpc 50051→50061 (targetPorts remain 4001/50051).
- grpc-health-probe ConfigMap: host updated to auth-service.off-campus-housing.svc.cluster.local:50061.

Colima cluster setup (scripts/setup-new-colima-cluster.sh)
-----------------------------------------------------------
- Script now only starts Colima + k3s and creates namespaces: ingress-nginx, envoy-test, off-campus-housing.
- MetalLB install removed from this script; document METALLB_POOL=192.168.64.260-192.168.64.270 in usage to avoid conflicts when installing MetalLB later.
- Next steps documented: install-metallb.sh, bring-up-external-infra.sh, build-and-load-auth-service.sh, then deploy.

Build and load auth-service into Colima k3s (scripts/build-and-load-auth-service.sh)
------------------------------------------------------------------------------------
- New script: build auth-service:dev from repo root, load into k3s via colima ssh + k3s ctr images import.
- Uses Colima Docker context when available; run after setup-new-colima-cluster.sh.

Backup script rename (7 DBs)
---------------------------
- backup-all-8-dbs.sh renamed to backup-all-7-dbs.sh (name aligned with all-7-* output and 7 DBs 5441–5447).
- backup-all-dbs.sh now delegates to backup-all-7-dbs.sh.
- ENGINEERING.md: RESTORE_BACKUP_DIR example all-8 → all-7.

bring-up-external-infra.sh
--------------------------
- Already correct: 7 Postgres services (5441–5447), backups/all-7-*; no code change.

Pgbench default namespace
-------------------------
- run_auth_pgbench_sweep.sh: default namespace record-platform → off-campus-housing (usage and NS).
- run_pgbench_sweep.sh: default namespace off-campus-housing-tracker → off-campus-housing (usage, NS, and fallbacks for deploy/rollout).

K8s config: proto aligned with repo proto/ (infra/k8s/base/config)
------------------------------------------------------------------
- kustomization.yaml: proto-files ConfigMap now uses housing protos only. Files live under config/proto/ and are synced from repo root proto/ (source of truth). Included: common, health, auth, listings, booking, messaging, notification, trust, analytics. Dropped records, social, shopping, auction-monitor, python-ai. Config namespace set to off-campus-housing.
- Replaced old config/proto/ copies with copies of repo proto/ (same set). Added config/proto/README.md with sync instructions (cp from repo proto/ after proto changes).

Service READMEs: gRPC implementation guidance + proto refs
-----------------------------------------------------------
- Each of auth-, listings-, booking-, messaging-, notification-, trust-, analytics-service README: added "Implementing this service (gRPC)" with (1) pointer to the service’s proto in repo proto/, (2) short "first time with gRPC" steps (proto = contract, generate code, implement handlers, register server), (3) which service/RPCs to implement and health.proto for probes.
- services/README.md: added note that gRPC contracts live in proto/ and each service README has implementation guidance; api-gateway bullet updated with link to api-gateway README and proto/.

api-gateway README
-----------------
- New services/api-gateway/README.md: describes gateway as single entrypoint; table of supported backends with proto file links (auth, listings, booking, messaging, notification, trust, analytics); pointer to common.proto and health.proto; link to each service’s implementation section for teammates new to gRPC.
