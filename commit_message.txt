feat(infra): make-it-real local stack, Caddy spine lock, k6 messaging

Local dev stack (no k3s)
------------------------
- docker-compose.local.yml: Postgres (messaging + media), Kafka TLS, Zookeeper, Redis, MinIO; init-db for schemas; cert volume.
- scripts/dev-generate-certs.sh: dev CA + leaf certs (messaging-service, media-service, kafka client/broker) under ./certs/.
- docs/LOCAL_DEV.md: run certs, compose up, init-db, MinIO bucket, unit/integration test commands.

Kafka strict TLS + media + messaging + analytics
-------------------------------------------------
- services/common: getKafkaSslConfigForTest(env), kafka.test.ts (missing cert → throw); no plaintext fallback.
- services/media-service: full scaffold (handlers, S3, mediaRepo, outbox, health), proto/certs paths via __dirname, objectKey unit tests.
- services/messaging-service: package.json, tsconfig; rateLimit.ts (Redis 30/min, 500/day).
- services/analytics-service: consumers/messagingConsumer.ts (MessageSentV1 → user_listing_engagement.messages_sent); infra/db/04-analytics-user-listing-engagement.sql.
- .github/workflows/local-integration.yml: generate certs, Kafka TLS test, compose up, init-db, media + event-layer-verification tests, tear down.

SNI + Caddy NodePort + k6 messaging
-----------------------------------
- Caddyfile: SNI comment (off-campus-housing.local only; diff from record platform).
- scripts/rollout-caddy.sh: CADDY_NODEPORT=30444 support (sed nodePort) for caddy-h3-service-nodeport.yaml and siblings.
- README: no-conflict note and CADDY_NODEPORT=30444 example.
- services/api-gateway: GET /api/messaging/healthz → messaging-service:4014; messaging in auth-bypass list.
- scripts/load/k6-messaging.js: load test /api/messaging/healthz; scripts/load/run-k6-phases.sh: messaging phase.

Caddy network spine (production-grade)
--------------------------------------
- Caddyfile: gRPC matcher by Content-Type application/grpc* (no path regex). REST by explicit path list /api/* /auth/* ... and not Content-Type application/grpc*. SNI: @wrong_host → 421. TLS ciphers (TLS_AES_128_GCM_SHA256, TLS_AES_256_GCM_SHA384, TLS_CHACHA20_POLY1305_SHA256). Tracing headers (X-Request-Id, Traceparent, Tracestate) to backends. Explicit @web path /*; :443 fallback returns 421 (no silent proxy). Global log format json.
- scripts/verify-caddy-grpc-routing.sh: readiness check that gRPC routes (grpcurl grpc.health.v1.Health/Check via Caddy).
- docs/CADDY_NETWORK_SPINE.md: discipline, logging, gRPC readiness.
