# PR #5 — Production edge transport, observability on-cluster, API contract fixes, listing → daily_metrics

**Standalone PR:** Use this file as the GitHub/GitLab description for **PR #5**. It stacks after **PR #4** (CI contract) and assumes housing k3s + Caddy + observability namespaces already exist.

**Title (suggested):** `feat(och): edge transport + Grafana subpath, Prometheus transport rules, gateway 404/listings 400, Kafka → daily_metrics`

**Stack position:** PR #4 = CI/certify · **PR #5 (this)** = **runtime parity**: same hostname for Grafana as the app, SLO/transport visibility, deterministic gateway/listings/analytics behavior for Playwright, and **ListingCreated** projection into `analytics.daily_metrics`.

---

## Executive summary

| Area | What |
|------|------|
| **Edge** | Caddy upstream keepalive/buffers; optional Envoy reference YAML; HTTP/3 concurrent `curl` stress script. |
| **Grafana** | File provisioning (dashboard provider + projected ConfigMaps); legacy dashboards unwrapped to Grafana 11 JSON; **OCH — transport / edge** dashboard; **Grafana served at `https://off-campus-housing.test/grafana/`** (no port-forward as primary path). |
| **Prometheus** | `och-transport-edge` recording rules + alerts (gateway 5xx/502, optional Envoy series); **`http_requests_total` uses `code` label** (aligned with prom-client); scrape **`envoy-test`** admin `/stats/prometheus`. |
| **API gateway** | Unknown `/api/*` paths → **404 before JWT** (typos are not 401). |
| **Listings** | Invalid UUID on `GET /listings/:id` → **400**; **HTTP create** publishes **ListingCreatedV1** to Kafka (same envelope as gRPC). |
| **Analytics** | **Kafka consumer** increments `daily_metrics.new_listings` (idempotent `processed_events`); payload **`listed_at_day`** aligns bucket with UI date. |
| **Build hygiene** | **`InstanceType<typeof Pool>`** for concurrency guards; **`.dockerignore` `**/*.tsbuildinfo`** so Docker builds are not poisoned by incremental TS state. |
| **Deploy ergonomics** | **`kubectl kustomize` fallback** in Makefile + `deploy-dev.sh`; **`rollout-restart-och-after-pool-tuning.sh`** restarts **envoy-test** + optional ingress **envoy**. |
| **Playwright** | Expect **404** unknown route; listings bad id **400**; analytics **healthz** poll; **daily-metrics** allows **502** during warmup; system-integrity uses **local calendar date** + **e2eApiBase()** for metrics URL. |

---

## What changed (by path)

### Edge & Caddy
- **`Caddyfile`**, **`infra/k8s/caddy-h3-configmap.yaml`** — `handle_path /grafana/*` → Grafana ClusterIP; API upstream tuning (encode, transport keepalive/buffers) kept in sync.
- **`infra/k8s/reference/envoy-quic-downstream.example.yaml`** — reference only if QUIC terminates on Envoy.

### Observability (K8s)
- **`infra/k8s/base/observability/grafana-dashboard-providers.yaml`** — file provider → `/var/lib/grafana/dashboards/default`.
- **`infra/k8s/base/observability/grafana-dashboards-transport.yaml`** — `och-transport-edge.json`.
- **`infra/k8s/base/observability/grafana-dashboards.yaml`** — top-level JSON for microservices / tracing / housing-uptime (schemaVersion 38).
- **`infra/k8s/base/observability/grafana-deploy.yaml`** — mount provider + **projected** `grafana-dashboards` + `grafana-dashboards-transport`; **`GF_SERVER_ROOT_URL`** + **`GF_SERVER_SERVE_FROM_SUB_PATH`**; image **`grafana/grafana:11.5.2`**.
- **`infra/k8s/base/observability/prometheus-deploy.yaml`** — job **`envoy-test-pods`**; rules volume unchanged.
- **`infra/k8s/base/observability/prometheus-rules-och-slo.yaml`** — `code` label fix for SLO recording rules; **`och-transport-edge`** group.
- **`infra/k8s/base/observability/grafana-dashboards.yaml`** / **`grafana-deploy.yaml`** / **`kustomization.yaml`** — wiring as above.
- **`infra/k8s/base/envoy-test/deploy.yaml`** — Prometheus scrape annotations on admin **15000**.
- **`infra/grafana/och-transport-edge.json`** — repo copy of transport dashboard (optional import).

### Scripts
- **`scripts/transport/http3-concurrent-curl-stress.sh`** — parallel `curl --http3-only` against health URL.
- **`scripts/rollout-restart-och-after-pool-tuning.sh`** — **envoy-test** + ingress **envoy** rollouts.
- **`scripts/deploy-dev.sh`**, **`Makefile`** — `kubectl kustomize` when `kustomize` binary missing.

### Services
- **`services/api-gateway/src/server.ts`** — unknown `/api/*` segment guard → 404.
- **`services/listings-service/src/listing-kafka.ts`** — shared Kafka producer for listing events.
- **`services/listings-service/src/grpc-server.ts`**, **`http-server.ts`** — publish **ListingCreatedV1** + **`listed_at_day`**; HTTP **GET** id UUID validation → 400.
- **`services/listings-service`**, **`trust-service`**, **`analytics-service`**, **`messaging-service`** — **`InstanceType<typeof Pool>`** on concurrency guard.
- **`services/analytics-service/src/consumers/listingEventsConsumer.ts`** — consumer + **`analytics-service/package.json`** **`kafkajs`**.
- **`services/analytics-service/src/server.ts`** — start listing consumer.

### Webapp E2E
- **`webapp/e2e/gateway.routing.spec.ts`**, **`listings.full.spec.ts`**, **`analytics.full.spec.ts`**, **`system-integrity.spec.ts`** — contract alignment as above.

### Other
- **`.dockerignore`** — `**/*.tsbuildinfo`.

---

## Deploy / verify (local k3s)

```bash
SERVICES="api-gateway listings-service analytics-service" bash scripts/build-housing-images-k3s.sh
kubectl kustomize infra/k8s/overlays/dev | kubectl apply -f -
bash scripts/rollout-caddy.sh
kubectl rollout restart deployment/grafana -n observability
kubectl rollout restart deployment/api-gateway deployment/listings-service deployment/analytics-service -n off-campus-housing-tracker
```

- **Grafana:** `https://off-campus-housing.test/grafana/` (default admin in manifest; change in prod).
- **Stress:** `OCH_H3_URL=… OCH_H3_CA=… CONCURRENCY=200 bash scripts/transport/http3-concurrent-curl-stress.sh`
- **E2E:** `bash scripts/run-playwright-e2e-preflight.sh` (requires Kafka for **daily_metrics** increment on HTTP listing create).

---

## Not in this PR (intentionally)

- Live **Caddy admin** dynamic HTTP/3 disable or **Envoy runtime** adaptive stream caps (control-plane product decision).
- **Replacing** all unrelated local **bench_logs** / **backup** churn — keep PR focused; exclude those paths from `git add` unless you intend them.

---

**Suggested merge note:** After merge, run one full image build + apply + rollouts on each environment so **Grafana env vars** and **new images** match.

Made with Tom.
