# Dependencies
node_modules/
.pnpm-store/

# webapp/ is part of the repo — do not add rules that ignore webapp/ or its contents
# (e.g. avoid ignoring webapp/ or **/webapp). Use webapp/.gitkeep so the folder is tracked.

# Build / dist
dist/
build/
*.tsbuildinfo

# Env and secrets
.env
.env.*
!.env.example

# IDE / OS
.DS_Store
*.log
.idea/
.vscode/

# Optional
coverage/
.nyc_output/
*.tgz
