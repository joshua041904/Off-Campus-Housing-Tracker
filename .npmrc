# pnpm workspace (optional; used by api-gateway Dockerfile)
shamefully-hoist=false

# Do not install optional native platform packages (stops @rollup/rollup-* optional deps if any slip through).
# If watch-mode / file watchers misbehave on macOS, temporarily comment this line.
optional=false

# Rollup 4 + Vitest/Vite: keep env hint for tools that read it (root override pins rollup → @rollup/wasm-node).
ROLLUP_DISABLE_NATIVE=true
