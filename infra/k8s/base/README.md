Base contains substrate only (namespaces, config, kafka-external, kafka, envoy-test, redis, haproxy, nginx, observability, monitoring, exporters). Add one directory per app service (e.g. api-gateway, auth-service, listings-service) with deploy.yaml and service.yaml, then add the resource name to kustomization.yaml. Replace record-platform namespace with your app namespace.
