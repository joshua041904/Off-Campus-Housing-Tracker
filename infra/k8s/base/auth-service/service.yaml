apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: off-campus-housing-tracker
  labels: { app: auth-service }
spec:
  selector: { app: auth-service }
  ports:
    - name: http
      port: 4011
      targetPort: http
    - name: grpc
      port: 50061
      targetPort: grpc
