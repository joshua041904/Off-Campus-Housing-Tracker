apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: off-campus-housing-tracker
  labels:
    app: booking-service
spec:
  selector:
    app: booking-service
  ports:
    - name: http
      port: 4013
      targetPort: 4013
    - name: grpc
      port: 50063
      targetPort: 50063
