# Reference Envoy static config (OCH gRPC services only). Live cluster uses ConfigMap in deploy.yaml.
static_resources:
  listeners:
  - name: listener_0
    address:
      socket_address:
        address: 0.0.0.0
        port_value: 10000
    filter_chains:
    - filters:
      - name: envoy.filters.network.http_connection_manager
        typed_config:
          "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
          stat_prefix: grpc_json
          codec_type: AUTO
          route_config:
            name: local_route
            virtual_hosts:
            - name: local_service
              domains: ["*"]
              routes:
              - match:
                  prefix: "/grpc.health.v1.Health/"
                route:
                  cluster: auth_service
              # Route by gRPC path prefix (e.g. /auth.AuthService/Register, /booking.BookingService/GetBooking)
              - match:
                  prefix: "/auth."
                route:
                  cluster: auth_service
              - match:
                  prefix: "/messaging."
                route:
                  cluster: messaging_service
              - match:
                  prefix: "/listings."
                route:
                  cluster: listings_service
              - match:
                  prefix: "/analytics."
                route:
                  cluster: analytics_service
              - match:
                  prefix: "/booking."
                route:
                  cluster: booking_service
              - match:
                  prefix: "/notification."
                route:
                  cluster: notification_service
              - match:
                  prefix: "/trust."
                route:
                  cluster: trust_service
              # Default route (fallback to auth for backward compatibility)
              - match:
                  prefix: "/"
                route:
                  cluster: auth_service
          http_filters:
          - name: envoy.filters.http.router
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
  clusters:
  # Auth Service (gRPC port 50061 — must match auth-service K8s Service)
  - name: auth_service
    connect_timeout: 5s
    type: LOGICAL_DNS
    lb_policy: ROUND_ROBIN
    http2_protocol_options: {}
    load_assignment:
      cluster_name: auth_service
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: auth-service.off-campus-housing-tracker.svc.cluster.local
                port_value: 50061
    transport_socket:
      name: envoy.transport_sockets.tls
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
        sni: off-campus-housing.test
        common_tls_context:
          validation_context:
            trusted_ca:
              filename: /etc/certs/ca/dev-root.pem
  
  # Messaging Service (port 50064)
  - name: messaging_service
    connect_timeout: 5s
    type: LOGICAL_DNS
    lb_policy: ROUND_ROBIN
    http2_protocol_options: {}
    load_assignment:
      cluster_name: messaging_service
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: messaging-service.off-campus-housing-tracker.svc.cluster.local
                port_value: 50064
    transport_socket:
      name: envoy.transport_sockets.tls
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
        sni: off-campus-housing.test
        common_tls_context:
          validation_context:
            trusted_ca:
              filename: /etc/certs/ca/dev-root.pem
  
  # Listings Service (port 50062)
  - name: listings_service
    connect_timeout: 5s
    type: LOGICAL_DNS
    lb_policy: ROUND_ROBIN
    http2_protocol_options: {}
    load_assignment:
      cluster_name: listings_service
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: listings-service.off-campus-housing-tracker.svc.cluster.local
                port_value: 50062
    transport_socket:
      name: envoy.transport_sockets.tls
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
        sni: off-campus-housing.test
        common_tls_context:
          validation_context:
            trusted_ca:
              filename: /etc/certs/ca/dev-root.pem
  
  # Analytics Service (housing gRPC 50067)
  - name: analytics_service
    connect_timeout: 5s
    type: LOGICAL_DNS
    lb_policy: ROUND_ROBIN
    http2_protocol_options: {}
    load_assignment:
      cluster_name: analytics_service
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: analytics-service.off-campus-housing-tracker.svc.cluster.local
                port_value: 50067
    transport_socket:
      name: envoy.transport_sockets.tls
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
        sni: off-campus-housing.test
        common_tls_context:
          validation_context:
            trusted_ca:
              filename: /etc/certs/ca/dev-root.pem
  
  # Booking Service (gRPC port 50063)
  - name: booking_service
    connect_timeout: 5s
    type: LOGICAL_DNS
    lb_policy: ROUND_ROBIN
    http2_protocol_options: {}
    load_assignment:
      cluster_name: booking_service
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: booking-service.off-campus-housing-tracker.svc.cluster.local
                port_value: 50063
    transport_socket:
      name: envoy.transport_sockets.tls
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
        sni: off-campus-housing.test
        common_tls_context:
          validation_context:
            trusted_ca:
              filename: /etc/certs/ca/dev-root.pem

  # Notification Service (gRPC port 50065)
  - name: notification_service
    connect_timeout: 5s
    type: LOGICAL_DNS
    lb_policy: ROUND_ROBIN
    http2_protocol_options: {}
    load_assignment:
      cluster_name: notification_service
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: notification-service.off-campus-housing-tracker.svc.cluster.local
                port_value: 50065
    transport_socket:
      name: envoy.transport_sockets.tls
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
        sni: off-campus-housing.test
        common_tls_context:
          validation_context:
            trusted_ca:
              filename: /etc/certs/ca/dev-root.pem

  # Trust Service (gRPC port 50066)
  - name: trust_service
    connect_timeout: 5s
    type: LOGICAL_DNS
    lb_policy: ROUND_ROBIN
    http2_protocol_options: {}
    load_assignment:
      cluster_name: trust_service
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: trust-service.off-campus-housing-tracker.svc.cluster.local
                port_value: 50066
    transport_socket:
      name: envoy.transport_sockets.tls
      typed_config:
        "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
        sni: off-campus-housing.test
        common_tls_context:
          validation_context:
            trusted_ca:
              filename: /etc/certs/ca/dev-root.pem