apiVersion: v1
kind: Service
metadata:
  name: redis-external
  namespace: record-platform
  labels:
    app: redis-external
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
---
apiVersion: v1
kind: Endpoints
metadata:
  name: redis-external
  namespace: record-platform
subsets:
  - addresses:
      # Docker Compose Redis container IP
      # Update if container restarts: docker inspect record-platform-redis-1 --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'
      - ip: 172.18.0.2  # record-platform-redis-1 container IP
    ports:
      - name: redis
        port: 6379

