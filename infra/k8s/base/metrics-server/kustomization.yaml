# metrics-server — part of bootstrap C.metrics (kubectl apply -k this path).
# Pin release for reproducibility; bump when upgrading metrics-server.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://github.com/kubernetes-sigs/metrics-server/releases/download/v0.7.2/components.yaml
