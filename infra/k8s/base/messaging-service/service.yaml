apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: off-campus-housing
  labels: { app: messaging-service }
spec:
  selector: { app: messaging-service }
  ports:
    - name: grpc
      port: 50064
      targetPort: grpc
