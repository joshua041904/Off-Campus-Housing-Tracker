apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: observability
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s
    rule_files:
      - /etc/prometheus/rules/och-slo.yml
      - /etc/prometheus/rules/kafka-health.yml
      - /etc/prometheus/rules/auth-outbox.yml
      - /etc/prometheus/rules/ollama-worker.yml
      - /etc/prometheus/rules/analytics-ai-slo.yml
      - /etc/prometheus/rules/ai-health-composite.yml
      - /etc/prometheus/rules/ai-control-plane.yml
      - /etc/prometheus/rules/ai-cluster-arbitration.yml
      - /etc/prometheus/rules/och-outbox.yml
      - /etc/prometheus/rules/coverage-integrity.yml
    scrape_configs:
      - job_name: 'prometheus'
        metrics_path: /prometheus/metrics
        static_configs:
          - targets: ['localhost:9090']
      - job_name: 'ollama'
        static_configs:
          - targets: ['ollama-gateway.off-campus-housing-tracker.svc.cluster.local:8081']
        metrics_path: /metrics
      # One job per housing service so Prometheus /targets shows clear job + health per app.
      - job_name: 'housing-api-gateway'
        metrics_path: /metrics
        static_configs:
          - targets: ['api-gateway.off-campus-housing-tracker.svc.cluster.local:4020']
            labels:
              och_scrape_role: app
              service: api-gateway
      - job_name: 'housing-auth-service'
        metrics_path: /metrics
        static_configs:
          - targets: ['auth-service.off-campus-housing-tracker.svc.cluster.local:4011']
            labels:
              och_scrape_role: app
              service: auth-service
      - job_name: 'housing-listings-service'
        metrics_path: /metrics
        static_configs:
          - targets: ['listings-service.off-campus-housing-tracker.svc.cluster.local:4012']
            labels:
              och_scrape_role: app
              service: listings-service
      - job_name: 'housing-booking-service'
        metrics_path: /metrics
        static_configs:
          - targets: ['booking-service.off-campus-housing-tracker.svc.cluster.local:4013']
            labels:
              och_scrape_role: app
              service: booking-service
      - job_name: 'housing-messaging-service'
        metrics_path: /metrics
        static_configs:
          - targets: ['messaging-service.off-campus-housing-tracker.svc.cluster.local:4014']
            labels:
              och_scrape_role: app
              service: messaging-service
      - job_name: 'housing-notification-service'
        metrics_path: /metrics
        static_configs:
          - targets: ['notification-service.off-campus-housing-tracker.svc.cluster.local:4015']
            labels:
              och_scrape_role: app
              service: notification-service
      - job_name: 'housing-trust-service'
        metrics_path: /metrics
        static_configs:
          - targets: ['trust-service.off-campus-housing-tracker.svc.cluster.local:4016']
            labels:
              och_scrape_role: app
              service: trust-service
      - job_name: 'housing-analytics-service'
        metrics_path: /metrics
        static_configs:
          - targets: ['analytics-service.off-campus-housing-tracker.svc.cluster.local:4017']
            labels:
              och_scrape_role: app
              service: analytics-service
      - job_name: 'housing-media-service'
        metrics_path: /metrics
        static_configs:
          - targets: ['media-service.off-campus-housing-tracker.svc.cluster.local:4018']
            labels:
              och_scrape_role: app
              service: media-service
      # Pushgateway: run scripts/coverage/push-och-metrics.sh after matrix (deploy Pushgateway in observability NS).
      - job_name: 'pushgateway'
        honor_labels: true
        static_configs:
          - targets: ['pushgateway.observability.svc.cluster.local:9091']
      # Dev fallback: HTTP exporter on host (node scripts/coverage/serve-coverage-integrity-prom.mjs).
      - job_name: 'och-coverage-integrity-host'
        scrape_interval: 60s
        metrics_path: /metrics
        static_configs:
          - targets: ['host.docker.internal:9464']
            labels:
              exporter: 'och-coverage-integrity'
      # All pods in the housing namespace that opt in via prometheus.io/* annotations
      # (correct HTTP/metrics port per deployment — avoids stale single-job ports).
      - job_name: 'housing-pods'
        kubernetes_sd_configs:
          - role: pod
            namespaces:
              names:
                - off-campus-housing-tracker
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
            action: keep
            regex: true
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
            action: replace
            target_label: __metrics_path__
            regex: (.+)
          - source_labels: [__address__, __meta_kubernetes_pod_annotation_prometheus_io_port]
            action: replace
            regex: ([^:]+)(?::\d+)?;(\d+)
            replacement: $1:$2
            target_label: __address__
          - action: labelmap
            regex: __meta_kubernetes_pod_label_(.+)
          - source_labels: [__meta_kubernetes_namespace]
            action: replace
            target_label: kubernetes_namespace
          - source_labels: [__meta_kubernetes_pod_name]
            action: replace
            target_label: kubernetes_pod_name
          - source_labels: [__meta_kubernetes_pod_label_app]
            action: replace
            target_label: service
      - job_name: 'envoy-test-pods'
        kubernetes_sd_configs:
          - role: pod
            namespaces:
              names:
                - envoy-test
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
            action: keep
            regex: true
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
            action: replace
            target_label: __metrics_path__
            regex: (.+)
          - source_labels: [__address__, __meta_kubernetes_pod_annotation_prometheus_io_port]
            action: replace
            regex: ([^:]+)(?::\d+)?;(\d+)
            replacement: $1:$2
            target_label: __address__
          - action: labelmap
            regex: __meta_kubernetes_pod_label_(.+)
          - source_labels: [__meta_kubernetes_namespace]
            action: replace
            target_label: kubernetes_namespace
          - source_labels: [__meta_kubernetes_pod_name]
            action: replace
            target_label: kubernetes_pod_name
          - source_labels: [__meta_kubernetes_pod_label_app]
            action: replace
            target_label: service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: observability
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      serviceAccountName: prometheus
      containers:
      - name: prometheus
        image: prom/prometheus:v2.52.0
        ports:
        - containerPort: 9090
        args:
          - '--config.file=/etc/prometheus/prometheus.yml'
          - '--storage.tsdb.path=/prometheus'
          # Caddy forwards /prometheus/* unchanged; Prometheus serves UI under this prefix.
          - '--web.external-url=https://off-campus-housing.test/prometheus/'
          - '--web.route-prefix=/prometheus/'
          # Store histogram exemplars (trace_id) from OpenMetrics scrapes (e.g. api-gateway).
          - '--enable-feature=exemplar-storage'
        volumeMounts:
        - name: config
          mountPath: /etc/prometheus
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
      volumes:
      - name: config
        projected:
          sources:
            - configMap:
                name: prometheus-config
                items:
                  - key: prometheus.yml
                    path: prometheus.yml
            - configMap:
                name: prometheus-rules-och-slo
                items:
                  - key: och-slo.yml
                    path: rules/och-slo.yml
            - configMap:
                name: prometheus-rules-kafka-health
                items:
                  - key: kafka-health.yml
                    path: rules/kafka-health.yml
            - configMap:
                name: prometheus-rules-auth-outbox
                items:
                  - key: auth-outbox.yml
                    path: rules/auth-outbox.yml
            - configMap:
                name: prometheus-rules-ollama-worker
                items:
                  - key: ollama-worker.yml
                    path: rules/ollama-worker.yml
            - configMap:
                name: prometheus-rules-analytics-ai-slo
                items:
                  - key: analytics-ai-slo.yml
                    path: rules/analytics-ai-slo.yml
            - configMap:
                name: prometheus-rules-ai-health-composite
                items:
                  - key: ai-health-composite.yml
                    path: rules/ai-health-composite.yml
            - configMap:
                name: prometheus-rules-ai-control-plane
                items:
                  - key: ai-control-plane.yml
                    path: rules/ai-control-plane.yml
            - configMap:
                name: prometheus-rules-ai-cluster-arbitration
                items:
                  - key: ai-cluster-arbitration.yml
                    path: rules/ai-cluster-arbitration.yml
            - configMap:
                name: prometheus-rules-och-outbox
                items:
                  - key: och-outbox.yml
                    path: rules/och-outbox.yml
            - configMap:
                name: prometheus-rules-coverage-integrity
                items:
                  - key: coverage-integrity.yml
                    path: rules/coverage-integrity.yml
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: observability
spec:
  selector:
    app: prometheus
  ports:
  - port: 9090
    targetPort: 9090
  type: ClusterIP
