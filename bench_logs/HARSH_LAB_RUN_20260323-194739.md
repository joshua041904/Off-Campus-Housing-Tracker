# Harsh lab run — started 2026-03-23 ~19:47 UTC (local 19:47)

## What was launched

| Component | Command / env |
|-----------|----------------|
| **Contention watcher** | `./scripts/perf/watch-cluster-contention.sh` → periodic `kubectl top` |
| **Preflight (harsher)** | `K6_SUITE_STABILITY_AGGRESSIVE=1` `K6_SUITE_COLIMA_DROP_CACHES=0` `K6_SUITE_FAIL_ON_NODE_CPU=0` |

`K6_SUITE_FAIL_ON_NODE_CPU=0` was set so the run can **finish** and still produce **`k6-suite-resources-*.log`** (CPU gate can abort early under load).

`K6_SUITE_COLIMA_DROP_CACHES=0` matches your snippet (no VM page-cache drop).

## Artifact paths (repo: `bench_logs/`)

| File | Purpose |
|------|---------|
| **`preflight-harsh-lab-20260323-194739-full.log`** | Main preflight tee (authoritative) |
| **`preflight-harsh-lab-20260323-194739-wrap.log`** | Duplicate of stdout from nohup wrapper |
| **`cluster-contention-watch-harsh-20260323-194715.log`** | Second-terminal style samples every ~8s |
| **`k6-suite-resources-<timestamp>.log`** | Created when step **7a** runs — k6 hook `kubectl top` append (**check after preflight reaches suites**) |
| **`telemetry-during-20260323-194739.log`** | Control-plane telemetry (repo root) |
| **`telemetry-after-20260323-194739.txt`** | Post-run snapshot (when preflight completes) |
| **`raw-metrics-20260323-194739.txt`** | Raw metrics (when complete) |

## Monitor / stop

```bash
# Follow preflight
tail -f bench_logs/preflight-harsh-lab-20260323-194739-full.log

# Follow contention samples
tail -f bench_logs/cluster-contention-watch-harsh-20260323-194715.log

# Find k6 resource log (after 7a)
ls -lt bench_logs/k6-suite-resources*.log
```

**Stop contention watcher** when preflight is done (saves noise):

```bash
pkill -f watch-cluster-contention.sh
```

## Note on PIDs

If `bench_logs/preflight-harsh-lab.pid` / `contention-watcher-harsh.pid` point at a parent `bash -c`, use:

```bash
pgrep -fl run-preflight-scale-and-all-suites
pgrep -fl watch-cluster-contention
```
